
# High Priority
* Build the `evaluation` module. The goal is to calculate statistical power / false detection rates under a wide variety of exam scenarios, figure out the limits of the current system.

# Medium Priority
* Turn the backend service into a Python package so that someone could run `import sherlock; sherlock.detect_cheaters(...)` in a Python script.

# Low Priority
* Migrate backend typechecking from mypy to ty
